# 한국어 표현 정책

사용자 응답과 문서는 한국 개발자가 한 번에 이해할 수 있는 자연스러운 한국어로 작성한다.
코드 식별자, 명령어, 경로처럼 원문이 정확해야 하는 문자열은 번역하지 않는다.

## 외래어 매핑 표

이 표는 `~/.claude/scripts/korean-style-check.sh`가 읽는 단일 소스다.

| 금지 | 권장 |
| --- | --- |
| 매트릭스 (matrix) | 표, 영향 표, 변경 표 |
| 트리아지 (triage) | 분류, 분류 작업 |
| 베이스라인 / baseline | 기준값, 기준점, 기준선 |
| 스파이크 (spike) | 사전 조사, 탐색 작업, 실증 |
| 게이트 (gate) | 점검, 사전 점검, 통과 조건 |
| wall-time / wall clock time | 전체 처리 시간, 실제 처리 시간 |
| silent failure | 묻혀버린 실패, 노출 안 되는 실패 |
| in-flight (요청) | 처리 중인 요청, 진행 중인 요청 |
| 리포지토리 위생 (repo hygiene) | 리포지토리 정리, gitignore 정비 |
| 카나리 (canary) | 인스턴스 배포 후 검증 |
| byte-level diff | 바이트 단위 비교 |
| ramp | 단계적 상향 |
| flat fan-out / fan-out | 통합 병렬 호출, 병렬 호출 |
| ephemeral (instance / runner) | 일회성 인스턴스·러너, 쓰고 버리는 |
| 클램프 / clamp | 값을 범위로 제한, 하한이나 상한으로 제한 |
| 폭주 (CPU 폭주 등) | 과점유, 과도한 점유 |
| 강등 (로그 레벨 강등) | 낮춤 |
| 오살 (stale PID 오살) | 엉뚱한 프로세스를 죽이는 문제 |
| 외과적 (surgical) | 정밀한, 국소적, 범위를 좁힌 |
| ingest | 적재 |
| 사전 소진 | 사전 해소 |
| 단일 진실원 | 단일 소스 |
| 변질 의심 | 변질 우려 |
| 패턴 답습 | 동일 패턴 적용, 그대로 적용 |

## 문장 구성

어색한 한국어와 장황한 한국어는 원인이 다르다.
어색함은 조사, 어미, 서술어 같은 문법 성분이 빠져서 생긴다.
장황함은 문장 수와 반복, 사족이 많아서 생긴다.
그러므로 문장은 성분을 갖춰 완결하게 쓰고, 대신 문장 수를 줄인다.

- 조사와 어미를 생략하지 않는다. 명사만 이어 붙여 독자가 관계를 짐작하게 만들지 않는다.
- 평문은 서술어와 종결어미로 끝낸다. 제목, 표, 목록 항목은 명사구로 끝내도 된다.
- 문맥에서 복원할 수 없는 주어와 목적어는 생략하지 않는다.
- `의`를 한 구에 두 번 겹치지 않는다. 겹친다는 것은 성분이 빠졌다는 신호다.
- 비유어로 일반 명사와 동사를 대체하지 않는다. 해당 분야에서 굳은 관용 표현은 그대로 쓴다.
- 엠대시로 절과 절을 잇지 않는다. 콜론이나 접속사로 바꾼다. 목록과 표에서 이름과 설명을 가르는 용도는 그대로 쓴다.

| 고칠 문장 | 빠진 것 | 고친 문장 |
| --- | --- | --- |
| 그러면 경고가 붙습니다. | 대상과 상황 | 이미 작업 중인 파일에도 경고 표시가 붙는다. |
| 지출 비용 추론 용도의 토큰 카운트 함수의 오류 상황에서 | 조사와 어미 | 지출한 비용을 추론하는 토큰 카운트 함수에 오류가 발생하면 |
| 컨텍스트 압축 전 신중 반영한다. | 서술 관계 | 컨텍스트가 압축되기 전에 신중하게 반영한다. |
| 코드로 박는 자리 | 비유어 | 코드에 명시하는 위치 |

이 축들은 검사기가 잡지 못한다. 사람과 모델이 출력 직전에 직접 본다.

## 문장을 늘어놓는 방식

여기까지가 문장 하나를 어떻게 만드는지였다면, 이 절은 문장을 어떻게 이어 놓는지다.
분량과 매체가 아니라 한국어 자체가 정하는 것만 둔다.

- 한 문장이나 의미 단위마다 줄을 나눈다. 한 줄에 한 호흡만 담는다.
- 본문에서 `A + B`, `A · B`, `A & B`처럼 항목을 이어 붙이지 않는다. 쉼표나 `와/과`, 목록을 쓴다.
- 괄호를 두 겹으로 겹치지 않는다. `((설명) 안의 설명)` 은 별도 문장으로 나눈다.
- `=` 나 `→` 로 동치와 인과를 압축하는 것은 한 단락에 한 번까지다. 반복되면 표로 바꾼다.
- `§` 를 쓰지 않는다. 한국어 문서에서 뜻이 바로 읽히지 않는다. `섹션`, `장` 또는 번호를 쓴다.

## 작성 원칙

- 표에 없는 생소한 외래어, 한자어, 비유어도 쉬운 말이나 실제 동작으로 풀어 쓴다.
- `rate limit`, `pipeline`, `backpressure`처럼 한국 개발자가 영문에 더 익숙한 용어는 그대로 둔다.
- 기술 문서의 `Latency`, `Throughput`, `Saturation`은 처음 한 번만 한국어를 병기한다.
- 줄이는 대상은 문장의 수와 반복이지 문장의 성분이 아니다. 짧게 만들려고 조사와 어미를 떼지 않는다.
- 작업 범위 밖의 기존 문체는 함께 고치지 않는다.
